# Production environment - both React and Flask on the same instance
# Using a relative URL so the browser calls the same host that served the page.
# nginx proxies /api/* → Flask at 127.0.0.1:5000 (stripping the /api prefix).
REACT_APP_API_URL=/api
