Flask
Flask-CORS
Flask-SQLAlchemy
Flask-Migrate
requests
python-dotenv
openai
anthropic
langchain
langchain-openai
langchain-anthropic
langchain-community
langchain-core
beautifulsoup4
faiss-cpu
PyMuPDF
chromadb
rake-nltk
numpy<2
scipy
nltk
pandas
typing-extensions
Werkzeug
psycopg2-binary
pgvector
boto3
python-docx
networkx
selenium
webdriver-manager
openpyxl
duckduckgo-search
google-auth-oauthlib
google-auth
google-api-python-client
psutil
celery[redis]
redis
gunicorn

# Workflow orchestration (Supplier Qualification Pipeline - agents/workflow_orchestration/)
# Pinned explicitly rather than left transitive: previously only pulled in
# incidentally via langchain's own dependency tree, which is not safe once
# this is load-bearing orchestration infra. Spiked 2026-08-22 against the
# full requirements.txt set on Python 3.11 (the Docker/CI target) - clean
# install, `pip check` reports no broken requirements.
# Note: langgraph-checkpoint-postgres depends on psycopg (v3), a different
# driver than psycopg2-binary used elsewhere in this file - both coexist
# fine (separate import namespaces), just don't assume they're the same.
langgraph==1.2.10
langgraph-checkpoint==4.1.1
langgraph-checkpoint-postgres==3.1.2
